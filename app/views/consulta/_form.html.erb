<%= form_with(model: consultum) do |form| %>
  <% if consultum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(consultum.errors.count, "error") %> prohibited this consultum from being saved:</h2>

      <ul>
        <% consultum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
  <%= form.label :medico_id, style: "display: block" %>
  <%= form.collection_select :medico_id, @medicos, :id, :nome, prompt: "Selecione um Médico" %>
</div>

<div>
  <%= form.label :paciente_id, style: "display: block" %>
  <%= form.collection_select :paciente_id, @pacientes, :id, :nome, prompt: "Selecione um Paciente" %>
</div>


  <div>
    <%= form.label :data_consulta, style: "display: block" %>
    <%= form.datetime_field :data_consulta %>
  </div>

  <div>
    <%= form.label :ficha, style: "display: block" %>
    <%= form.text_field :ficha %>
  </div>

  <div>
    <%= form.label :tipo, style: "display: block" %>
    <%= form.text_field :tipo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
